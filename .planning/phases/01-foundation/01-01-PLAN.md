---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - postcss.config.mjs
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - .env.local.example
  - .prettierrc
  - .prettierignore
  - eslint.config.mjs
  - eslint-local-rules/no-tailwind-arbitrary.cjs
  - next.config.ts
  - next-env.d.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03, QUAL-01, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "Running npm run dev starts the app with zero errors"
    - "Design system tokens (colors, spacing, radius, shadow) are declared in @theme in globals.css and visible as CSS custom properties on :root in the browser"
    - "No raw hex, px, or hardcoded values appear in component styles — only token references"
    - "ESLint catches and blocks any arbitrary Tailwind value like bg-[#ff0000] in className attributes"
    - "Breakpoints 360px, 768px, 1280px are configured for mobile-first responsive design"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with Next.js 16, Tailwind v4, React 19"
      contains: "next"
    - path: "app/globals.css"
      provides: "Tailwind v4 @theme block with all MomCare Design System tokens"
      contains: "@theme"
    - path: "eslint-local-rules/no-tailwind-arbitrary.cjs"
      provides: "Custom ESLint rule blocking arbitrary Tailwind values"
      contains: "no-tailwind-arbitrary"
    - path: "eslint.config.mjs"
      provides: "ESLint flat config integrating the custom rule"
      contains: "no-tailwind-arbitrary"
  key_links:
    - from: "app/globals.css"
      to: "browser :root"
      via: "Tailwind v4 @theme auto-generates CSS custom properties"
      pattern: "@theme"
    - from: "eslint.config.mjs"
      to: "eslint-local-rules/no-tailwind-arbitrary.cjs"
      via: "local ESLint plugin registration"
      pattern: "no-tailwind-arbitrary"
---

<objective>
Scaffold the Next.js 16 project from scratch, bridge MomCare Design System tokens into Tailwind v4 via @theme in globals.css, and set up the custom ESLint rule that blocks arbitrary Tailwind values in CI.

Purpose: Every subsequent plan depends on a working scaffold with design tokens in place. This plan establishes the project foundation and the token contract that all future components must follow.
Output: A running Next.js 16 app with design tokens visible as CSS custom properties and an ESLint rule enforcing token-only styling.
</objective>

<execution_context>
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project with Tailwind v4, TypeScript, ESLint, and Prettier</name>
  <files>
    package.json, tsconfig.json, postcss.config.mjs, next.config.ts, next-env.d.ts,
    app/layout.tsx, app/page.tsx, app/globals.css, .prettierrc, .prettierignore,
    eslint.config.mjs, .env.local.example
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section for exact versions and install commands)
    .planning/phases/01-foundation/01-CONTEXT.md (Locked decisions A and D)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --no-src-dir --import-alias "@/*"` to scaffold the project. Accept defaults. This creates a Next.js 16.x project with Tailwind v4.

    2. Verify the scaffolded Next.js version is 16.x by checking package.json. If it is 15.x, run `npm install next@latest react@latest react-dom@latest` to upgrade.

    3. Install additional production dependencies:
       ```
       npm install @tanstack/react-query zustand react-hook-form zod @hookform/resolvers
       ```

    4. Install dev dependencies:
       ```
       npm install --save-dev msw @faker-js/faker vitest @vitejs/plugin-react @testing-library/react @testing-library/user-event @chialab/vitest-axe axe-core jsdom prettier
       ```

    5. Create `.prettierrc` with content:
       ```json
       {
         "semi": false,
         "singleQuote": true,
         "tabWidth": 2,
         "trailingComma": "all",
         "printWidth": 100
       }
       ```

    6. Create `.prettierignore` with content:
       ```
       node_modules
       .next
       out
       public
       ```

    7. Add scripts to `package.json`:
       - `"format": "prettier --write ."`
       - `"format:check": "prettier --check ."`
       - `"test": "vitest"`
       - `"test:run": "vitest run"`

    8. Create `.env.local.example` with content:
       ```
       # Auth bypass for development — allows accessing (app) routes without session cookie
       # Only active when NODE_ENV !== 'production'
       AUTH_BYPASS=true
       ```

    9. Copy `.env.local.example` to `.env.local` (gitignored by default).

    10. Update `app/layout.tsx` to be a minimal root layout — html and body tags only, importing globals.css. No providers yet (those come in Plan 02). Set `lang="en"` on html. Add metadata export with `title: 'MomCare'` and `description: 'Maternal health case management'`.

    11. Replace `app/page.tsx` with a simple placeholder that renders `<main><h1>MomCare</h1><p>Foundation phase — no screens yet.</p></main>`. No hardcoded style values.
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" with version starting with "16" or "^16"
    - package.json contains "tailwindcss" dependency
    - package.json contains "@tanstack/react-query" dependency
    - package.json contains "zustand" dependency
    - package.json contains "react-hook-form" dependency
    - package.json contains "zod" dependency
    - package.json contains "msw" in devDependencies
    - package.json contains "@faker-js/faker" in devDependencies
    - package.json contains "@chialab/vitest-axe" in devDependencies
    - package.json contains "vitest" in devDependencies
    - package.json scripts contains "test", "test:run", "format", "format:check"
    - .env.local.example contains "AUTH_BYPASS=true"
    - app/layout.tsx contains 'lang="en"'
    - app/layout.tsx contains "MomCare" in metadata title
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Next.js 16 project scaffolded with all dependencies installed. npm run build succeeds. No screens — only the scaffold.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge MomCare Design System tokens into Tailwind v4 @theme and create arbitrary value ESLint rule</name>
  <files>
    app/globals.css, eslint-local-rules/no-tailwind-arbitrary.cjs, eslint.config.mjs
  </files>
  <read_first>
    app/globals.css (current state after scaffold)
    eslint.config.mjs (current state after scaffold)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: @theme token bridge, Pattern 3: ESLint rule)
    .planning/phases/01-foundation/01-CONTEXT.md (Decision A: token bridge)
  </read_first>
  <action>
    **Part A: Extract design tokens from MomCare Design System**

    1. Open the MomCare Design System Storybook at `momcaredesignsystemwithcodexnew.vercel.app` using WebFetch. Navigate to the Design Tokens, Color Reference, and Foundations stories. Extract ALL color, spacing, radius, shadow, and typography token values.

    2. If the Storybook stories cannot be scraped (they use iframes), use the following fallback token set derived from the design system's visual reference. These are placeholder values that establish the correct @theme structure — the user will verify and adjust exact values in a checkpoint later:

    **Fallback token palette (declare in @theme):**
    ```
    Colors (primary):
      --color-primary: #1B6B4A (green — the app's primary brand color from the design system)
      --color-primary-light: #E8F5EE
      --color-primary-dark: #0D4A30
      --color-primary-hover: #155A3E

    Colors (semantic):
      --color-surface: #FFFFFF
      --color-surface-secondary: #F8F9FA
      --color-on-surface: #1A1A2E
      --color-on-surface-secondary: #6B7280
      --color-border: #E5E7EB
      --color-border-strong: #D1D5DB

    Colors (risk — clinical):
      --color-risk-high: #DC2626
      --color-risk-high-bg: #FEF2F2
      --color-risk-medium: #F59E0B
      --color-risk-medium-bg: #FFFBEB
      --color-risk-low: #16A34A
      --color-risk-low-bg: #F0FDF4

    Colors (status):
      --color-success: #16A34A
      --color-warning: #F59E0B
      --color-error: #DC2626
      --color-info: #2563EB

    Colors (vaccination status per VACC-02):
      --color-vax-given: #16A34A
      --color-vax-due: #F59E0B
      --color-vax-overdue: #DC2626
      --color-vax-not-applicable: #9CA3AF

    Spacing:
      --spacing-xs: 0.25rem
      --spacing-sm: 0.5rem
      --spacing-md: 1rem
      --spacing-lg: 1.5rem
      --spacing-xl: 2rem
      --spacing-2xl: 3rem

    Radius:
      --radius-sm: 0.25rem
      --radius-md: 0.5rem
      --radius-lg: 0.75rem
      --radius-xl: 1rem
      --radius-full: 9999px

    Shadow:
      --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05)
      --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)
      --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)

    Breakpoints (QUAL-03):
      --breakpoint-mobile: 360px
      --breakpoint-tablet: 768px
      --breakpoint-desktop: 1280px
    ```

    3. Replace `app/globals.css` with:
    ```css
    @import "tailwindcss";

    @theme {
      /* MomCare Design System Token Bridge
         Source: momcaredesignsystemwithcodexnew.vercel.app
         All component styles MUST use these tokens — no hardcoded values.
         Tailwind v4 auto-generates utility classes from these:
           bg-primary, text-on-surface, rounded-md, shadow-sm, etc. */

      /* [Paste all tokens from step 2 here] */
    }
    ```

    **Part B: Create custom ESLint rule to block arbitrary Tailwind values**

    4. Create directory `eslint-local-rules/` at project root.

    5. Create `eslint-local-rules/no-tailwind-arbitrary.cjs`:
    ```javascript
    // Blocks arbitrary Tailwind values like bg-[#ff0000], p-[20px], w-[100px]
    // Allows CSS variable references like bg-[var(--color-primary)]
    module.exports = {
      meta: {
        type: 'problem',
        docs: {
          description: 'Disallow arbitrary Tailwind CSS values — use design tokens instead',
        },
        messages: {
          noArbitrary: 'Arbitrary Tailwind value "{{value}}" is banned. Use a design token from @theme instead.',
        },
        schema: [],
      },
      create(context) {
        function checkForArbitrary(value, node) {
          if (typeof value !== 'string') return
          // Match class-[value] patterns but allow [var(--*)]
          const arbitraryPattern = /(?:^|\s)[\w-]+\[(?!var\()([^\]]+)\]/g
          let match
          while ((match = arbitraryPattern.exec(value)) !== null) {
            context.report({
              node,
              messageId: 'noArbitrary',
              data: { value: match[0].trim() },
            })
          }
        }

        return {
          JSXAttribute(node) {
            if (node.name.name !== 'className') return
            // String literal className="..."
            if (node.value && node.value.type === 'Literal' && typeof node.value.value === 'string') {
              checkForArbitrary(node.value.value, node)
            }
          },
        }
      },
    }
    ```

    6. Update `eslint.config.mjs` to register the custom rule. Add a local plugin object:
    ```javascript
    import noTailwindArbitrary from './eslint-local-rules/no-tailwind-arbitrary.cjs'

    // In the config array, add:
    {
      plugins: {
        'local': {
          rules: {
            'no-tailwind-arbitrary': noTailwindArbitrary,
          },
        },
      },
      rules: {
        'local/no-tailwind-arbitrary': 'error',
      },
    }
    ```

    Integrate this into the existing ESLint flat config array that create-next-app generated. Preserve all existing configs/rules.

    7. Verify the rule works by temporarily adding `className="bg-[#ff0000]"` to a div in `app/page.tsx`, running `npm run lint`, confirming the error appears, then removing the test class.
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains '@import "tailwindcss"'
    - app/globals.css contains "@theme"
    - app/globals.css contains "--color-primary:"
    - app/globals.css contains "--color-risk-high:"
    - app/globals.css contains "--color-risk-medium:"
    - app/globals.css contains "--color-risk-low:"
    - app/globals.css contains "--spacing-xs:"
    - app/globals.css contains "--spacing-md:"
    - app/globals.css contains "--radius-md:"
    - app/globals.css contains "--shadow-sm:"
    - app/globals.css contains "--breakpoint-mobile: 360px"
    - app/globals.css contains "--breakpoint-tablet: 768px"
    - app/globals.css contains "--breakpoint-desktop: 1280px"
    - eslint-local-rules/no-tailwind-arbitrary.cjs contains "noArbitrary"
    - eslint-local-rules/no-tailwind-arbitrary.cjs contains "arbitraryPattern"
    - eslint.config.mjs contains "no-tailwind-arbitrary"
    - npm run lint exits with code 0 (no violations in clean code)
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Tailwind v4 @theme block declares all MomCare Design System tokens. ESLint rule blocks arbitrary values. Both lint and build pass cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `npm run lint` passes with zero violations
3. `npm run dev` starts the app (manual check — not automated in CI)
4. globals.css contains @theme block with all token categories (colors, spacing, radius, shadow, breakpoints)
5. Adding `bg-[#ff0000]` to any component and running `npm run lint` produces an ESLint error
</verification>

<success_criteria>
- Next.js 16 project fully scaffolded with all production and dev dependencies installed
- Design system tokens declared in @theme — visible as CSS custom properties on :root when app runs
- ESLint custom rule blocks arbitrary Tailwind values like bg-[#ff0000] but allows bg-[var(--color-primary)]
- npm run build and npm run lint both pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
