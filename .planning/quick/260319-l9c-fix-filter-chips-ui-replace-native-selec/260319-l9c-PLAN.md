---
quick_id: 260319-l9c
title: "Fix filter chips UI — replace native selects with custom dropdown chips"
tasks: 1
---

# Quick Task 260319-l9c

## Objective

Replace ugly native `<select>` dropdown elements in the Mothers screen filter row with custom-styled FilterChip components that match the MomCare design system.

## Task 1: Replace native selects with FilterChip component

**Files:** `app/dashboard/mothers/page.tsx`

**Action:**
1. Create `FilterChip` component with: pill-shaped button, ChevronDown icon, dropdown panel on click
2. Active state: primary-light background, primary border, X button to clear
3. Inactive state: white surface, gray border, chevron-down icon
4. Dropdown panel: white surface, shadow-md, border, max-height with scroll
5. Click-outside-to-close via useRef + useEffect
6. Replace all 4 native `<select>` elements (Clinic, Area, Trimester, Risk Level)
7. Update Clear All button with X icon and risk-high-bg background
