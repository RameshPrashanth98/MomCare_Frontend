---
phase: quick
plan: 260322-i4b
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/clinics/add/page.tsx
  - app/dashboard/clinics/page.tsx
autonomous: true
requirements: [QUICK-260322-i4b]

must_haves:
  truths:
    - "Add Clinic Session form screen renders with 4 card sections matching wireframe"
    - "Clinic Schedule 'Add Clinic Session' button navigates to /dashboard/clinics/add"
    - "Back arrow on Add Clinic Session returns to Clinic Schedule"
    - "Cancel and Create Clinic Session buttons visible in fixed bottom bar"
    - "Bottom nav shows CLINICS tab as active"
  artifacts:
    - path: "app/dashboard/clinics/add/page.tsx"
      provides: "Add Clinic Session form screen"
      min_lines: 150
  key_links:
    - from: "app/dashboard/clinics/page.tsx"
      to: "/dashboard/clinics/add"
      via: "Link on Add Clinic Session button"
      pattern: "href.*clinics/add"
---

<objective>
Build the Add Clinic Session form screen at `/dashboard/clinics/add` with 4 card-style form sections (Clinic Information, Schedule Details, Staff Assignment, Expected Attendance), a fixed bottom action bar with Cancel/Create buttons, and wire navigation from the Clinic Schedule page's "Add Clinic Session" button.

Purpose: Allow healthcare staff to create new clinic sessions from the Clinics flow.
Output: Fully styled form screen matching wireframe, navigable from Clinic Schedule.
</objective>

<execution_context>
@.planning/quick/260322-i4b-build-add-clinic-session-screen-with-for/260322-i4b-PLAN.md
</execution_context>

<context>
@app/dashboard/clinics/page.tsx (existing Clinic Schedule — copy header/nav patterns, wire Add button)
@app/globals.css (design tokens — use var(--color-*), var(--spacing-*), var(--radius-*))
@lib/types/entities.ts (ClinicSession type for field reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Add Clinic Session form screen</name>
  <files>app/dashboard/clinics/add/page.tsx</files>
  <action>
Create a 'use client' page component at app/dashboard/clinics/add/page.tsx. Follow the EXACT styling patterns from app/dashboard/clinics/page.tsx (inline style objects with design tokens, Tailwind utility classes only for flex/grid layout, not for token values).

HEADER (copy pattern from clinics/page.tsx):
- Back arrow (ArrowLeft) using router.back(), title "Add Clinic Session"
- Right side: Search icon, "B/EN" language badge (styled like EN badge on clinics page but showing "B/EN"), pink avatar circle "RP" (28px round, brand-pink bg, white text)
- Height 56px, border-bottom, same padding as clinics page header

SCROLLABLE CONTENT — 4 white card sections on var(--color-surface-secondary) background. Each section card:
- White bg, border: 1px solid var(--color-border), borderRadius: var(--radius-xl)
- Margin: var(--spacing-md) horizontal, var(--spacing-sm) vertical
- Padding: var(--spacing-md)
- Section header: uppercase text, fontSize 11px, fontWeight 600, color var(--color-brand-pink), letterSpacing 0.5px, marginBottom var(--spacing-md)

SECTION 1 — CLINIC INFORMATION:
- "Clinic Name" label (fontSize 13px, fontWeight 500, color var(--color-on-surface), marginBottom 6px)
- Select dropdown styled as input (height 44px, border 1px solid var(--color-border), borderRadius var(--radius-lg), paddingLeft/Right var(--spacing-md), fontSize 14px, color var(--color-on-surface-secondary) for placeholder, bg var(--color-surface), width 100%, appearance none). Use native <select> with a placeholder option "Select Clinic Name"
- "Clinic Type" — same select style but with pink border highlight (border-color: var(--color-brand-pink)) and pre-filled text "Antenatal / Postnatal" in var(--color-on-surface) color
- "Clinic Location" — text input, same input styling, placeholder "Enter clinic location"
- "PHM Area" — select dropdown, placeholder "Select PHM Area"
- Gap between form fields: var(--spacing-md)

SECTION 2 — SCHEDULE DETAILS:
- "Clinic Date" — input with Calendar icon on right side (position absolute, right 12px), placeholder "Select Date". Use type="text" with readOnly for visual-only (no actual date picker needed, this is a UI build)
- "Clinic Start Time" — input with Clock icon on right, placeholder "Select Start Time"
- "Clinic End Time" — input with Clock icon on right, placeholder "Select End Time"
- Input wrapper needs position relative for icon positioning

SECTION 3 — STAFF ASSIGNMENT:
- "Assigned Midwife" — navigation-style row: left label "Assigned Midwife" (13px, 500 weight), right side shows "Select Midwife" in secondary color + ChevronRight icon. Full row is a button/div with height 44px, border, rounded, flex justify-between align-center, paddingLeft/Right var(--spacing-md)
- "Supervising MOH / AMOH" — same navigation row style, "Select Officer" + ChevronRight
- These look like tappable rows that would navigate to a selection screen

SECTION 4 — EXPECTED ATTENDANCE:
- "Expected Number of Mothers" label
- Textarea or larger text input, placeholder "Enter notes", minHeight 80px, same border/radius styling, resize none

FIXED BOTTOM ACTION BAR (above bottom nav):
- Height 68px, bg var(--color-surface), borderTop 1px solid var(--color-border), flex items-center justify-between, paddingLeft/Right var(--spacing-md)
- "Cancel" button: outlined/text style, border 1.5px solid var(--color-border-strong), borderRadius var(--radius-full), height 44px, paddingLeft/Right 24px, fontSize 14px, fontWeight 500, color var(--color-on-surface-secondary), bg transparent. onClick router.back()
- "Create Clinic Session" button: filled pink, bg var(--color-brand-pink), color white, borderRadius var(--radius-full), height 44px, paddingLeft/Right 24px, fontSize 14px, fontWeight 600, border none

BOTTOM NAV BAR — Copy the exact NAV_ITEMS array and bottom nav JSX from clinics/page.tsx (CLINICS tab active). This is the inline pattern used across all pages.

Overall page background: var(--color-surface-secondary) on the scrollable area.

Import icons from lucide-react: ArrowLeft, Search, Calendar, Clock, ChevronRight, Home, Users, Building2, FileText, UserCircle.
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Add Clinic Session page renders at /dashboard/clinics/add with all 4 form sections, fixed bottom action bar, and bottom nav matching wireframe layout and design tokens</done>
</task>

<task type="auto">
  <name>Task 2: Wire navigation from Clinic Schedule to Add Clinic Session</name>
  <files>app/dashboard/clinics/page.tsx</files>
  <action>
In app/dashboard/clinics/page.tsx, change the "+ Add Clinic Session" button (around line 460-482) from a plain button to a Next.js Link component wrapping the same styled element. The button should navigate to "/dashboard/clinics/add".

Two options (choose whichever is cleaner):
1. Wrap the existing button in a Link: `<Link href="/dashboard/clinics/add" style={{ textDecoration: 'none', display: 'block', width: 'calc(100% - 2 * var(--spacing-md))', marginLeft: 'var(--spacing-md)', ... }}>` and keep the inner styles
2. Change the button to an anchor via Link: replace `<button>` with `<Link href="/dashboard/clinics/add">` keeping the same style object

Ensure the Link import is already present (it is, used for nav items and notifications). Keep all existing styles on the Add Clinic Session element (dashed border, pink color, rounded, etc.) exactly as they are.
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>"+ Add Clinic Session" button on Clinic Schedule page navigates to /dashboard/clinics/add when tapped</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `/dashboard/clinics/add` route exists and renders the form
- `/dashboard/clinics` "Add Clinic Session" button links to `/dashboard/clinics/add`
</verification>

<success_criteria>
- Add Clinic Session screen displays 4 card sections with correct form fields matching wireframe
- All form inputs use design tokens from globals.css (no hardcoded colors/spacing)
- Fixed bottom bar shows Cancel and Create Clinic Session buttons
- Bottom nav shows CLINICS tab active
- Navigation from Clinic Schedule "Add Clinic Session" button works
- Back arrow returns to previous page
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/quick/260322-i4b-build-add-clinic-session-screen-with-for/260322-i4b-SUMMARY.md`
</output>
