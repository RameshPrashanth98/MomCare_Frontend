---
phase: quick
plan: 260318-u2t
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(auth)/layout.tsx
  - app/(auth)/login/page.tsx
  - app/onboarding/page.tsx
autonomous: true
requirements: [AUTH-01]
must_haves:
  truths:
    - "Navigating to /login renders the login screen matching the wireframe"
    - "Onboarding Get Started and Skip buttons navigate to /login"
    - "Password field toggles visibility via eye icon"
    - "Clicking Login sets a session cookie and redirects to /dashboard"
  artifacts:
    - path: "app/(auth)/layout.tsx"
      provides: "Centered card layout shell for auth screens"
    - path: "app/(auth)/login/page.tsx"
      provides: "Full login screen matching wireframe"
  key_links:
    - from: "app/onboarding/page.tsx"
      to: "/login"
      via: "router.push('/login')"
      pattern: "router\\.push.*login"
    - from: "app/(auth)/login/page.tsx"
      to: "/dashboard"
      via: "router.push('/dashboard') after setting session cookie"
      pattern: "router\\.push.*dashboard"
---

<objective>
Build the login screen at app/(auth)/login/page.tsx matching the provided wireframe exactly, using only design tokens from globals.css. Create the (auth) route group layout shell. Verify onboarding buttons already wire to /login (they do — no changes needed to onboarding).

Purpose: Staff members see a polished login screen after completing or skipping onboarding, and can "log in" (mock auth) to reach the dashboard.
Output: Working login screen with password toggle, remember-me checkbox, mock login action, and decorative elements per wireframe.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@app/globals.css (design tokens — the ONLY source for colors, spacing, radius, shadows)
@app/layout.tsx (root layout with Providers)
@app/onboarding/page.tsx (onboarding screens — Get Started and Skip already call router.push('/login'))
@store/ui.ts (Zustand UI store — has isAuthenticated and setAuthenticated)

<interfaces>
<!-- From store/ui.ts — use for mock auth state -->
```typescript
export const useUIStore = create<UIStore>((set) => ({
  isAuthenticated: false,
  setAuthenticated: (v: boolean) => set({ isAuthenticated: v }),
  // ...other UI state
}))
```

<!-- From app/globals.css @theme — key tokens for this screen -->
```css
--color-brand-pink: #E8527A;
--color-brand-pink-dark: #C93A62;
--color-brand-pink-light: #FF85A8;
--color-surface: #FFFFFF;
--color-surface-secondary: #F8F9FA;
--color-on-surface: #1A1A2E;
--color-on-surface-secondary: #6B7280;
--color-border: #E5E7EB;
--color-splash-bg-center: #FFFFFF;
--color-splash-bg-mid: #FFE8F0;
--color-splash-bg-edge: #FFCDE0;
--spacing-xs: 0.25rem;  --spacing-sm: 0.5rem;  --spacing-md: 1rem;
--spacing-lg: 1.5rem;   --spacing-xl: 2rem;     --spacing-2xl: 3rem;
--radius-sm: 0.25rem;   --radius-md: 0.5rem;    --radius-lg: 0.75rem;
--radius-xl: 1rem;      --radius-full: 9999px;
--shadow-sm: ...;       --shadow-md: ...;        --shadow-lg: ...;
```
</interfaces>

**IMPORTANT — onboarding wiring is already correct:**
In `app/onboarding/page.tsx`, line 491: `const goToLogin = () => router.push('/login')`.
The `onGetStarted` prop on FeatureScreen calls `goToLogin`. The `onSkip` prop calls `goToLogin`.
NO changes needed to the onboarding file for button routing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create (auth) route group layout shell</name>
  <files>app/(auth)/layout.tsx</files>
  <action>
Create `app/(auth)/layout.tsx` as a server component (no 'use client') that provides a centered layout for auth screens.

Layout structure:
- Outer div: `min-h-dvh w-full flex items-center justify-center` with the same radial gradient background used by onboarding/splash: `background: radial-gradient(ellipse at center, var(--color-splash-bg-center) 0%, var(--color-splash-bg-mid) 50%, var(--color-splash-bg-edge) 100%)`
- Inner container: `w-full max-w-[393px] min-h-dvh` — constrains content to iPhone 16 Pro width
- Children rendered inside the inner container

Use inline style for the radial gradient (same pattern as onboarding/page.tsx line 504-506) since Tailwind cannot express multi-stop radial gradients via utility classes. All other styling via Tailwind utility classes using design token values.

Export as default function AuthLayout with `children: React.ReactNode` prop.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>app/(auth)/layout.tsx exists, TypeScript compiles, provides centered container for login page</done>
</task>

<task type="auto">
  <name>Task 2: Build login screen matching wireframe</name>
  <files>app/(auth)/login/page.tsx</files>
  <action>
Create `app/(auth)/login/page.tsx` as a 'use client' component. This is the complete login screen matching the wireframe. Use ONLY design token CSS variables from globals.css — no hardcoded hex values.

**Component structure (top to bottom, matching wireframe):**

1. **Language selector** (top-right): Small text link showing a Globe icon + "EN". Positioned absolute top-right. Uses `Globe` from lucide-react (size 14). No real functionality — decorative placeholder. Style: `text-[var(--color-on-surface-secondary)]` font-size ~12px.

2. **Decorative pink blob**: A large soft circle, positioned absolute, top-center offset slightly right. Use a div with `w-[200px] h-[200px] rounded-full` and `background: var(--color-brand-pink-light)` with `opacity: 0.3` and `filter: blur(40px)`. Position: `absolute top-[-40px] right-[20px]`.

3. **Pink circular plus button**: Centered, ~72px circle with `bg-[var(--color-brand-pink)]` and white Plus icon (lucide-react `Plus`, size 32, strokeWidth 2.5). Add subtle shadow using `var(--shadow-md)`.

4. **Heading**: "Maternal Health Clinic Management" — Use a serif font class. Style: `text-[22px] font-bold text-center text-[var(--color-on-surface)]` with `font-family: serif` (inline style for serif since no serif token exists). Two lines, centered.

5. **Subheading**: "CLINIC SYSTEM . SRI LANKA" — Small caps, letter-spaced. Style: `text-[11px] uppercase tracking-[2px] text-[var(--color-on-surface-secondary)]`.

6. **Staff ID input**: Full-width input with `User` icon (lucide-react) on the left. Placeholder: "Enter Staff ID". Container: `flex items-center gap-[var(--spacing-sm)] bg-[var(--color-surface)] border border-[var(--color-border)] rounded-[var(--radius-lg)] px-[var(--spacing-md)] h-[52px]`. Icon color: `var(--color-on-surface-secondary)`. Input: transparent bg, no border, flex-1, placeholder text `var(--color-on-surface-secondary)`.

7. **Password input**: Same styling as Staff ID input, but with `Lock` icon on left and a toggle button on right. The toggle button shows `Eye` or `EyeOff` (lucide-react) based on visibility state. Use `useState` for `showPassword` boolean. Input type toggles between "password" and "text".

8. **Remember me checkbox**: A row with a native checkbox input and label "Remember me". Style: `flex items-center gap-[var(--spacing-sm)]` with label in `text-[14px] text-[var(--color-on-surface)]`.

9. **Login button**: Full-width pill button. Style: `w-full h-[52px] rounded-[var(--radius-full)] bg-[var(--color-brand-pink)] text-white font-semibold text-[16px]` with shadow `var(--shadow-md)`.

10. **Links row**: "Forgot Password" | "Contact Administrator" — centered, separated by " | ". Color: `var(--color-brand-pink)`. Font-size: 13px. onClick: `console.log('Forgot password')` and `console.log('Contact admin')` respectively.

11. **Info card**: Light bordered card with text "Maternal Health Clinic System helps healthcare workers manage maternal records, clinic schedules, and antenatal care efficiently." Style: `border border-[var(--color-border)] rounded-[var(--radius-lg)] p-[var(--spacing-md)] text-center text-[13px] text-[var(--color-on-surface-secondary)] bg-[var(--color-surface)]`.

12. **Footer**: Small MoH logo placeholder (a 28px circle with `bg-[var(--color-brand-pink)]` and text "MoH" inside, font-size 8px, white), then "Ministry of Health - Sri Lanka" text, then "Version 1.0". All centered, muted color `var(--color-on-surface-secondary)`, font-size 11px.

**Login action (handleLogin function):**
- Prevent default form submission
- Import `useUIStore` from `@/store/ui` and call `setAuthenticated(true)`
- Set a cookie: `document.cookie = 'momcare-session=staff-demo; path=/; max-age=86400'`
- Import `useRouter` from `next/navigation` and call `router.push('/dashboard')`
- No validation needed — any input (or empty) triggers the mock login

**Spacing between sections:**
Use `var(--spacing-*)` tokens for all margins/gaps. Approximate layout gaps:
- After language selector: `mt-[var(--spacing-2xl)]` to the blob area
- Plus button to heading: `mt-[var(--spacing-md)]`
- Heading to subheading: `mt-[var(--spacing-xs)]`
- Subheading to first input: `mt-[var(--spacing-xl)]`
- Between inputs: `gap-[var(--spacing-md)]` (use flex column with gap)
- Inputs to remember-me: `mt-[var(--spacing-sm)]`
- Remember-me to login button: `mt-[var(--spacing-lg)]`
- Login button to links: `mt-[var(--spacing-md)]`
- Links to info card: `mt-[var(--spacing-xl)]`
- Info card to footer: `mt-[var(--spacing-lg)]`
- Footer bottom padding: `pb-[var(--spacing-xl)]`

**Overall page container:**
- `relative flex flex-col items-center w-full overflow-hidden`
- Padding: `px-[var(--spacing-xl)] pt-[var(--spacing-lg)]`

All icons from lucide-react: `User`, `Lock`, `Eye`, `EyeOff`, `Globe`, `Plus`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
- Login screen renders at /login matching the wireframe layout
- Password visibility toggle works (Eye/EyeOff swap)
- Login button sets session cookie and calls router.push('/dashboard')
- All styling uses design token CSS variables only — no hardcoded hex/px outside tokens
- Onboarding Get Started and Skip buttons navigate to /login (already wired, no changes needed)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. Visit /login — screen renders matching wireframe layout
4. Click eye icon on password field — toggles between password dots and plain text
5. Click Login — redirects to /dashboard (page may 404, that is expected)
6. Visit /onboarding — click through to last screen — "Get Started" navigates to /login
</verification>

<success_criteria>
- Login screen at /login renders all 12 wireframe sections in correct order
- Password toggle works (show/hide)
- Login button sets session cookie and redirects to /dashboard
- All colors, spacing, radius, shadows use design token CSS variables
- TypeScript compiles, build passes
- Onboarding buttons correctly navigate to /login (pre-existing, verified)
</success_criteria>

<output>
After completion, create `.planning/quick/260318-u2t-build-login-screen-matching-wireframe-wi/260318-u2t-SUMMARY.md`
</output>
