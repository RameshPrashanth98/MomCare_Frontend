---
phase: quick
plan: 260322-mv5
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/types/entities.ts
  - lib/mock/factories/lab-report.factory.ts
  - lib/mock/db.ts
  - app/dashboard/records/[id]/lab-reports/page.tsx
  - app/dashboard/records/page.tsx
autonomous: true
requirements: [quick-lab-reports]
must_haves:
  truths:
    - "Lab Reports screen renders at /dashboard/records/[id]/lab-reports with mother info card, filter chips, and report cards"
    - "Filter chips toggle between All Reports, Blood Tests, Urine Tests, Ultrasound and filter the list accordingly"
    - "Lab Reports button on Search Records page navigates to the lab-reports screen for that mother"
    - "Back button returns to /dashboard/records"
  artifacts:
    - path: "lib/types/entities.ts"
      provides: "LabReportType and LabReport interface"
      contains: "LabReport"
    - path: "lib/mock/factories/lab-report.factory.ts"
      provides: "createLabReport factory function"
      exports: ["createLabReport"]
    - path: "lib/mock/db.ts"
      provides: "labReports collection in db export"
      contains: "labReports"
    - path: "app/dashboard/records/[id]/lab-reports/page.tsx"
      provides: "Lab Reports screen component"
      min_lines: 100
  key_links:
    - from: "app/dashboard/records/page.tsx"
      to: "/dashboard/records/${mother.id}/lab-reports"
      via: "router.push onClick on Lab Reports button"
      pattern: "lab-reports"
    - from: "app/dashboard/records/[id]/lab-reports/page.tsx"
      to: "lib/mock/db.ts"
      via: "db.labReports filtered by motherId"
      pattern: "db\\.labReports"
---

<objective>
Build the Lab Reports screen at `/dashboard/records/[id]/lab-reports` with filter chips, report cards, and mock data layer.

Purpose: Allow healthcare staff to view a mother's lab reports (blood tests, urine tests, ultrasound) with filtering, following the same layout patterns as vaccination and health-tracking screens.
Output: Working Lab Reports screen with data layer, filter functionality, and navigation wiring from Search Records.
</objective>

<execution_context>
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@lib/types/entities.ts
@lib/mock/db.ts
@lib/mock/factories/vaccination.factory.ts
@lib/mock/seed.ts
@app/dashboard/records/page.tsx
@app/dashboard/records/[id]/health-tracking/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LabReport type, factory, and db collection</name>
  <files>lib/types/entities.ts, lib/mock/factories/lab-report.factory.ts, lib/mock/db.ts</files>
  <action>
1. In `lib/types/entities.ts`, append after the existing types:
```typescript
export type LabReportType = 'blood-test' | 'urine-test' | 'ultrasound'

export interface LabReport {
  id: string
  motherId: string
  type: LabReportType
  reportName: string
  date: string // ISO date
  uploadedBy: string
  createdByStaffId: string
}
```

2. Create `lib/mock/factories/lab-report.factory.ts` following the vaccination.factory.ts pattern:
- Import `faker` from `../seed` and `LabReport, LabReportType` from `../../types/entities`
- Map type to reportName: `'blood-test'` -> `'Blood Test Report'`, `'urine-test'` -> `'Urine Test Report'`, `'ultrasound'` -> `'Ultrasound Scan'`
- `uploadedBy` randomly picks from staff name lookup OR one of `['Clinic Staff', 'Radiology Unit', 'Lab Technician']`
- `date` uses `faker.date.recent({ days: 365 })` formatted as ISO date string (split T)
- Export `createLabReport(motherId: string, staffId: string, overrides?: Partial<LabReport>): LabReport`

3. In `lib/mock/db.ts`:
- Add import: `import type { ..., LabReport } from '../types/entities'` (add LabReport to existing import)
- Add import: `import { createLabReport } from './factories/lab-report.factory'`
- After the weightRecords block, add labReports generation: 3-6 reports per mother using `faker.number.int({ min: 3, max: 6 })`, with `faker.helpers.arrayElement(staffIds)` for staffId
- Add `labReports` to the `db` export object
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>LabReport type exists in entities.ts, factory generates seeded lab reports, db.labReports is populated and exported with no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Build Lab Reports page and wire navigation from Search Records</name>
  <files>app/dashboard/records/[id]/lab-reports/page.tsx, app/dashboard/records/page.tsx</files>
  <action>
1. Create `app/dashboard/records/[id]/lab-reports/page.tsx` as a `'use client'` component following the health-tracking page pattern exactly:

**Header:** Same as health-tracking — "Lab Reports" title with ArrowLeft back button linking to `/dashboard/records`, EN badge, Globe icon, profile avatar circle.

**Mother Info Card:** Same pattern as health-tracking/vaccination — avatar with initials, name, risk badge (High Risk / Normal), NIC, Trimester, Risk level, Midwife name. Look up mother from `db.mothers.find(m => m.id === id)` using `useParams().id`.

**Filter Chips Row:** Horizontally scrollable row with `overflow-x: auto` and `white-space: nowrap`.
- State: `const [activeFilter, setActiveFilter] = useState<'all' | LabReportType>('all')`
- Chips: `[{ value: 'all', label: 'All Reports' }, { value: 'blood-test', label: 'Blood Tests' }, { value: 'urine-test', label: 'Urine Tests' }, { value: 'ultrasound', label: 'Ultrasound' }]`
- Active chip style: `background: var(--color-on-surface)`, `color: white`, `border: none`
- Inactive chip style: `background: var(--color-surface)`, `color: var(--color-on-surface)`, `border: 1px solid var(--color-border)`
- All chips: `borderRadius: var(--radius-full)`, `padding: 6px 16px`, `fontSize: 13px`, `fontWeight: 500`, `cursor: pointer`

**Section Header:** "Lab Reports" in pink (`color: var(--color-brand-pink)`, `fontSize: 15px`, `fontWeight: 600`)

**Filtered Reports List:**
- `const filteredReports = useMemo(() => { const all = db.labReports.filter(r => r.motherId === id); return activeFilter === 'all' ? all : all.filter(r => r.type === activeFilter) }, [id, activeFilter])`
- Each card: white background, rounded, border, padding, flex row layout
- Left: colored circle (40x40, `background: var(--color-risk-high-bg)`) containing icon in pink (`color: var(--color-brand-pink)`)
  - Icon mapping: `blood-test` -> `TestTube2`, `urine-test` -> `FlaskConical`, `ultrasound` -> `CircleDot` (all from lucide-react)
- Center (flex-1): report name (bold 14px), date formatted as "DD Mon YYYY" using `formatDate`, "Uploaded by: {uploadedBy}" in secondary color 12px
- Right: "View Report >" text link in pink, fontSize 12px, fontWeight 500

**Upload Button:** At bottom of list, dashed border button:
- `border: 1.5px dashed var(--color-brand-pink)`, `background: transparent`, `color: var(--color-brand-pink)`
- `borderRadius: var(--radius-lg)`, full width, height 48px, flex center
- Plus icon + "Upload Lab Report" text

**Bottom Nav:** Same NAV_ITEMS pattern as records page with RECORDS active.

2. In `app/dashboard/records/page.tsx`, update the ACTION_BUTTONS onClick handler:
- Find the existing onClick conditional chain (lines ~361-363)
- Add: `btn.label === 'Lab Reports' ? () => router.push(\`/dashboard/records/\${mother.id}/lab-reports\`) :`
- Place it after the 'Health Tracking' condition and before the final `undefined`

**Import notes:** Import `TestTube2, FlaskConical, CircleDot` from lucide-react in the lab-reports page. Use the same helper functions (getInitials, getTrimester, getTrimesterOrdinal, getRiskLabel, getAssignedMidwifeName, formatDate) — copy them into the file following the health-tracking page pattern (each page is self-contained).
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npx tsc --noEmit --pretty 2>&1 | head -20 && npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>Lab Reports page renders with mother info, filter chips toggle correctly between report types, report cards display with correct icons per type, Upload button shows at bottom, Search Records "Lab Reports" button navigates to the correct URL, back button returns to /dashboard/records, build succeeds with no errors</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `npx next build` succeeds
- Navigate to `/dashboard/records`, search a mother, click "Lab Reports" button -> arrives at lab-reports page
- Mother info card displays correctly with name, risk badge, NIC, trimester, midwife
- Filter chips: "All Reports" active by default shows all cards; clicking "Blood Tests" filters to blood-test type only; same for other filters
- Each report card shows correct icon (TestTube2/FlaskConical/CircleDot), name, date, uploaded by
- Back arrow navigates to /dashboard/records
- Bottom nav shows RECORDS as active
</verification>

<success_criteria>
- Lab Reports screen fully renders matching wireframe layout
- Filter chips correctly filter report cards by type
- Navigation wired: Search Records -> Lab Reports -> back to Search Records
- Mock data layer: LabReport type, factory, and db collection all working
- TypeScript compilation and Next.js build pass cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260322-mv5-build-lab-reports-screen-with-filter-but/260322-mv5-SUMMARY.md`
</output>
