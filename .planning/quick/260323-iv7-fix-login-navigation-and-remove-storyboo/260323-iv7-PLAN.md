---
phase: quick
plan: 260323-iv7
type: execute
wave: 1
depends_on: []
files_modified:
  - middleware.ts
  - app/(auth)/login/page.tsx
  - package.json
  - eslint.config.mjs
  - .github/workflows/ci.yml
  - .claude/settings.local.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Clicking Login on the login page navigates to the dashboard/home screen"
    - "No Storybook-related code, config, or dependencies remain in the project"
    - "Application builds successfully after all changes"
    - "ESLint runs without errors after Storybook removal"
  artifacts:
    - path: "middleware.ts"
      provides: "Auth middleware with correct cookie name"
      contains: "momcare-session"
    - path: "package.json"
      provides: "Clean dependencies without Storybook"
  key_links:
    - from: "app/(auth)/login/page.tsx"
      to: "middleware.ts"
      via: "cookie name must match"
      pattern: "momcare-session"
---

<objective>
Fix the login-to-dashboard navigation bug and remove all Storybook code from the repository.

Purpose: The login button fails on Vercel because the cookie name set in login (momcare-session) does not match what middleware checks for (session). Storybook is moving to a separate repo and all traces must be removed.
Output: Working login flow + clean codebase without Storybook
</objective>

<execution_context>
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix login cookie name mismatch in middleware</name>
  <files>middleware.ts</files>
  <action>
    In middleware.ts line 22, change `request.cookies.get('session')` to `request.cookies.get('momcare-session')`.

    Root cause: login/page.tsx sets cookie named `momcare-session` but middleware checks for `session`. This mismatch means the middleware never finds the session cookie and always redirects authenticated users back to /login.

    Do NOT change the cookie name in login/page.tsx — `momcare-session` is the correct application-specific name. The middleware must be updated to match.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>middleware.ts checks for cookie named "momcare-session" — login sets this cookie and router.push('/dashboard') will succeed because middleware no longer redirects back to /login</done>
</task>

<task type="auto">
  <name>Task 2: Remove all Storybook code and configuration</name>
  <files>package.json, eslint.config.mjs, .github/workflows/ci.yml, .claude/settings.local.json</files>
  <action>
    Remove all Storybook traces from the project:

    1. **Delete directories:**
       - `stories/` (entire directory — contains Button, Header, Page stories/components, Configure.mdx, foundation/TokenDemo, assets/)
       - `.storybook/` (entire directory — contains main.ts, preview.ts)

    2. **package.json — remove scripts:**
       - Remove `"storybook": "storybook dev -p 6006"`
       - Remove `"build-storybook": "storybook build"`

    3. **package.json — remove devDependencies:**
       - `storybook`
       - `@storybook/nextjs-vite`
       - `@chromatic-com/storybook`
       - `@storybook/addon-vitest`
       - `@storybook/addon-a11y`
       - `@storybook/addon-docs`
       - `@storybook/addon-onboarding`
       - `eslint-plugin-storybook`

    4. **eslint.config.mjs:**
       - Remove line 1 (storybook comment)
       - Remove line 2 (`import storybook from "eslint-plugin-storybook"`)
       - Remove line 20 (`'storybook-static/**'` from globalIgnores)
       - Remove line 35 (`...storybook.configs["flat/recommended"]`)

    5. **.github/workflows/ci.yml:**
       - Remove the entire `storybook:` job (lines 44-53)

    6. **.claude/settings.local.json:**
       - Remove the two storybook-related entries from the allowedTools array:
         - `"Bash(npx storybook@latest init --type nextjs --yes)"`
         - `"Bash(npx storybook:*)"`

    7. **Run `npm install`** to regenerate package-lock.json without Storybook packages.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>No Storybook references in package.json, eslint config, CI workflow, or settings. stories/ and .storybook/ directories deleted. Lint and build pass cleanly.</done>
</task>

</tasks>

<verification>
- `grep -ri "storybook" --include="*.{ts,tsx,js,mjs,json,yml}" . | grep -v node_modules | grep -v package-lock` returns no results
- `npm run build` succeeds
- `npm run lint` succeeds
- middleware.ts contains `momcare-session` cookie check
</verification>

<success_criteria>
- Login button click sets momcare-session cookie and navigates to /dashboard without being redirected back
- Zero Storybook references in source files (excluding node_modules and package-lock.json)
- `npm run build` and `npm run lint` both pass
</success_criteria>

<output>
After completion, create `.planning/quick/260323-iv7-fix-login-navigation-and-remove-storyboo/260323-iv7-SUMMARY.md`
</output>
