---
phase: quick
plan: 260319-k1r
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/notifications/page.tsx
  - app/dashboard/page.tsx
autonomous: true
requirements: [QUICK-260319-k1r]

must_haves:
  truths:
    - "Tapping the Bell icon on the home screen navigates to /dashboard/notifications"
    - "Notification screen displays header with back arrow, title, filter icon, EN badge, avatar"
    - "Filter chips (All, High-Risk Alerts, Appointment Reminders) are horizontally scrollable"
    - "Four notification cards render with correct icon, color, title, time, details, and action button"
    - "Back arrow navigates to previous page (home screen)"
  artifacts:
    - path: "app/dashboard/notifications/page.tsx"
      provides: "Notification screen with header, filter tabs, and 4 notification cards"
      min_lines: 100
  key_links:
    - from: "app/dashboard/page.tsx"
      to: "app/dashboard/notifications/page.tsx"
      via: "Bell icon wrapped in Next.js Link to /dashboard/notifications"
      pattern: "Link.*href.*notifications"
---

<objective>
Build the Notification screen matching the wireframe with dummy data and navigation wiring.

Purpose: Users can navigate from home to notifications and back, viewing high-risk alerts, appointment reminders, vaccine reminders, and system alerts.
Output: Working /dashboard/notifications page with back navigation.
</objective>

<execution_context>
@.planning/quick/260319-k1r-build-notification-screen-matching-wiref/260319-k1r-PLAN.md
</execution_context>

<context>
@app/globals.css (design tokens — all styles MUST use these CSS custom properties)
@app/dashboard/page.tsx (home screen — Bell icon needs Link wrapper, follow same styling patterns)
@app/dashboard/layout.tsx (shared layout — 393px max-width iPhone 16 Pro shell)
</context>

<interfaces>
<!-- Existing patterns from dashboard/page.tsx that executor must follow -->

Styling pattern: inline `style={{ }}` with CSS custom properties like `var(--color-primary)`, `var(--spacing-md)`, `var(--radius-xl)`. Tailwind classes used only for flex/grid layout utilities.

Section label pattern:
```tsx
<div style={{
  fontSize: '12px', fontWeight: 600,
  textTransform: 'uppercase', letterSpacing: '0.08em',
  color: 'var(--color-on-surface-secondary)',
}}>
```

Icon import: `import { ... } from 'lucide-react'`

Dashboard layout shell: `max-w-[393px]` centered container with `var(--color-surface-secondary)` background.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Notification screen page</name>
  <files>app/dashboard/notifications/page.tsx</files>
  <action>
Create `app/dashboard/notifications/page.tsx` as a 'use client' component. Follow the exact same styling patterns as `app/dashboard/page.tsx` (inline styles with CSS custom properties, lucide-react icons, no hardcoded color values).

**Header (56px height, same as home top bar):**
- Left: Back arrow icon (ArrowLeft from lucide-react) — wrap in `Link` from `next/link` with `href="/dashboard"` to go back to home
- Center: "Notifications" title (fontSize 18px, fontWeight 700, color var(--color-on-surface))
- Right group: Filter icon (SlidersHorizontal from lucide-react), "EN" text badge (fontSize 12px, color var(--color-on-surface-secondary)), user avatar circle "M" (32px, background var(--color-brand-pink), white text, rounded-full)

**Filter chips row (horizontal scroll, hide scrollbar same pattern as high-risk mothers scroll):**
- Three chips: "All" (active — filled background var(--color-primary), white text), "High-Risk Alerts" (outlined — border 1px solid var(--color-border), color var(--color-on-surface-secondary)), "Appointment Reminders" (outlined same)
- Chips: fontSize 13px, paddingLeft/Right var(--spacing-md), paddingTop/Bottom var(--spacing-xs), borderRadius var(--radius-full), whiteSpace nowrap
- Row: paddingLeft/Right var(--spacing-md), gap var(--spacing-sm), marginTop var(--spacing-sm)

**Section label "NOTIFICATION LIST":**
- Use the exact section label pattern from dashboard (12px, 600 weight, uppercase, 0.08em letter-spacing, color var(--color-on-surface-secondary))
- marginTop var(--spacing-lg), paddingLeft/Right var(--spacing-md), marginBottom var(--spacing-sm)

**Notification cards — define dummy data array at top of file, then map over it. Each card:**
- White background (var(--color-surface)), borderRadius var(--radius-xl), padding var(--spacing-md), boxShadow var(--shadow-sm), marginBottom var(--spacing-sm), marginLeft/Right var(--spacing-md)

Card 1 — High Risk Alert:
- Icon: AlertTriangle in a 40px circle with background var(--color-risk-high-bg)
- Title: "High Risk Alert" in color var(--color-risk-high), fontWeight 600, fontSize 14px
- Time: "10 minutes ago" in fontSize 12px, color var(--color-on-surface-secondary)
- Details: "Mother: Nadeesha Silva", "Risk: High Blood Pressure", "Assigned Midwife: Malani" — each line fontSize 13px, color var(--color-on-surface-secondary)
- Button: "View" — filled green button (background var(--color-primary), white text, borderRadius var(--radius-full), fontSize 13px, padding var(--spacing-xs) var(--spacing-lg))

Card 2 — Appointment Reminder:
- Icon: Calendar in a 40px circle with background var(--color-surface-secondary)
- Title: "Appointment Reminder" in color var(--color-primary), fontWeight 600
- Time: "1 hour ago"
- Details: "Samanthi Perera", "Clinic: Clinic A", "Time: 9:15 AM Today"
- Button: "Mark as Read" — outlined (border 1px solid var(--color-border), color var(--color-on-surface-secondary), borderRadius var(--radius-full))

Card 3 — Vaccine Reminder:
- Icon: Syringe in a 40px circle with background var(--color-risk-low-bg)
- Title: "Vaccine Reminder" in color var(--color-warning), fontWeight 600
- Time: "5 hours ago"
- Details: "Baby: Kavindu", "Next Vaccine: Pentavalent 2", "Due Date: 25 Aug"
- Button: "Mark as Read" — outlined same as Card 2

Card 4 — System Alert:
- Icon: Bell in a 40px circle with background var(--color-risk-medium-bg)
- Title: "System Alert" in color var(--color-risk-high), fontWeight 600
- Time: "1 day ago"
- Details: "Monthly clinic report submission due tomorrow" (single line)
- Button: "Mark as Read" — outlined same as Card 2

Each card layout: top row is icon circle + title + time (time right-aligned or below title). Detail lines below. Button at bottom-right of card.

The page must scroll vertically within the dashboard layout shell. Use the same `flex flex-col h-dvh overflow-hidden` + `flex-1 overflow-y-auto` pattern from the dashboard page (but without bottom nav or action row — just scrollable content).
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Notification screen renders at /dashboard/notifications with header, 3 filter chips, section label, and 4 notification cards matching wireframe layout and colors. All styles use design system tokens only.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Bell icon on home screen to Notification page</name>
  <files>app/dashboard/page.tsx</files>
  <action>
In `app/dashboard/page.tsx`:

1. Add `import Link from 'next/link'` at the top (after existing imports).

2. Find the Bell icon in the Top App Bar section (line ~74). Wrap ONLY the Bell icon in a Next.js `Link` component:
```tsx
<Link href="/dashboard/notifications">
  <Bell size={22} strokeWidth={1.5} style={{ color: 'var(--color-brand-pink)' }} />
</Link>
```

Do NOT change any other part of the dashboard page. Keep all existing styles, layout, and data intact.
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Tapping the Bell icon on the home screen navigates to /dashboard/notifications. Back arrow on notification screen navigates back to /dashboard.</done>
</task>

</tasks>

<verification>
1. `next build` succeeds with no errors
2. Navigate to /dashboard — Bell icon is tappable and routes to /dashboard/notifications
3. Notification screen shows header with back arrow, "Notifications" title, filter icon, EN badge, avatar
4. Three filter chips display horizontally (All active/filled, others outlined)
5. Four notification cards render with correct colors, icons, details, and buttons
6. Back arrow returns to /dashboard
7. All colors and spacing use CSS custom properties from globals.css @theme block — no hardcoded hex values
</verification>

<success_criteria>
- /dashboard/notifications renders the notification screen matching the wireframe
- Bell icon on home screen navigates to notifications
- Back arrow navigates to home
- All 4 card types display with correct icon, color scheme, details, and action button
- Design system tokens used exclusively (no hardcoded values)
- iPhone 16 Pro (393px) layout respected via shared dashboard layout
</success_criteria>

<output>
After completion, create `.planning/quick/260319-k1r-build-notification-screen-matching-wiref/260319-k1r-SUMMARY.md`
</output>
