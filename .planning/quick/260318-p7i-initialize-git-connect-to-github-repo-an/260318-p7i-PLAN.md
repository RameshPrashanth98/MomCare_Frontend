---
phase: quick
plan: 260318-p7i
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "Remote origin points to https://github.com/RameshPrashanth98/MomCare_Frontend.git"
    - "All existing commits on main are visible on GitHub"
  artifacts: []
  key_links:
    - from: "local main branch"
      to: "origin/main"
      via: "git push -u origin main"
      pattern: "git push"
---

<objective>
Connect the existing local git repository to the GitHub remote and push all commits.

Purpose: Makes the codebase available on GitHub so it can be shared, backed up, and used for future CI/CD.
Output: Remote origin set; all local commits visible on GitHub under the main branch.
</objective>

<execution_context>
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

The local repo already has git initialised with commits on branch `main`.
No remote has been added yet. The target remote is:
  https://github.com/RameshPrashanth98/MomCare_Frontend.git
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add remote origin and push main</name>
  <files></files>
  <action>
    Run the following commands in sequence from the repo root
    (D:\UX projects\MomCare frontend):

    1. Add the remote:
       git remote add origin https://github.com/RameshPrashanth98/MomCare_Frontend.git

    2. Verify the remote was added:
       git remote -v

    3. Push all commits and set upstream tracking:
       git push -u origin main

    If step 1 fails with "remote origin already exists", skip it and
    proceed directly to step 3.

    If step 3 is rejected (non-fast-forward), DO NOT force push.
    Stop and report the conflict to the user — the remote may already
    contain commits that must be reconciled first.
  </action>
  <verify>
    <automated>git remote -v && git log --oneline -5 && git status</automated>
  </verify>
  <done>
    `git remote -v` shows origin pointing to
    https://github.com/RameshPrashanth98/MomCare_Frontend.git for both
    fetch and push. `git push` exits 0. `git status` reports
    "Your branch is up to date with 'origin/main'".
  </done>
</task>

</tasks>

<verification>
After the push completes, confirm the remote shows all commits:
  git log --oneline origin/main | head -10
Output should list the same commits as the local log.
</verification>

<success_criteria>
- Remote origin is set to https://github.com/RameshPrashanth98/MomCare_Frontend.git
- All local commits on main are present on origin/main
- Local branch tracks origin/main (git status shows "up to date")
</success_criteria>

<output>
No SUMMARY.md required for quick tasks.
</output>
