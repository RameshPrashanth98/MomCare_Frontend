---
phase: quick
plan: 260318-r1d
type: execute
wave: 1
depends_on: []
files_modified:
  - app/onboarding/page.tsx
  - app/globals.css
autonomous: false

must_haves:
  truths:
    - "All 4 onboarding screens render at 393x852px iPhone 16 Pro viewport"
    - "Welcome screen (step 0) matches the high-fidelity design with app icon, branding, illustration card, category badge, title, description, pill button, and dots"
    - "Screens 1-3 follow the wireframe layout: medical cross icon, title, illustration, description, Get Started button, dots, Skip/Next/Back navigation"
    - "Swipe/tap navigation works across all 4 screens with correct button logic"
    - "Design tokens from globals.css are used (no hardcoded colors)"
  artifacts:
    - path: "app/onboarding/page.tsx"
      provides: "Complete 4-screen onboarding flow matching designs"
      min_lines: 80
    - path: "app/globals.css"
      provides: "Any additional tokens or animations needed"
  key_links:
    - from: "app/onboarding/page.tsx"
      to: "/login"
      via: "router.push on final Get Started or Skip"
      pattern: "router\\.push.*login"
---

<objective>
Rewrite the onboarding page to match the provided design images as closely as possible.

The Welcome Screen (step 0) has a high-fidelity design showing: a large rounded-square app icon with gradient and medical cross, "MomCare" + "CLINIC MANAGEMENT" branding, an illustration inside a rounded card with light background, a "Maternal Healthcare" category badge, large title, description, pink pill-shaped "Get Started" button with arrow, and pagination dots. Decorative pink circles appear in the top-right and bottom areas.

Screens 1-3 follow wireframe layouts: medical cross icon at top, title, illustration area, description, rounded "Get Started" button, pagination dots, and Skip/Next/Back text navigation at the bottom.

Purpose: Make the onboarding UI match the actual designs provided by the user.
Output: Rewritten onboarding page matching designs at iPhone 16 Pro (393x852) dimensions.
</objective>

<execution_context>
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ramesh Prashanth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@app/globals.css
@app/onboarding/page.tsx
@components/onboarding/illustrations/IllustrationWelcome.tsx
@components/onboarding/illustrations/IllustrationMothers.tsx
@components/onboarding/illustrations/IllustrationSessions.tsx
@components/onboarding/illustrations/IllustrationHealth.tsx

Design reference images (READ THESE before implementing):
- C:/Users/Ramesh Prashanth/Downloads/Welcome Screen - iPhone 16 Pro.png (HIGH-FIDELITY -- match this closely)
- C:/Users/Ramesh Prashanth/Downloads/Onboarding 1.png (wireframe reference)
- C:/Users/Ramesh Prashanth/Downloads/Onboarding 2.png (wireframe reference)
- C:/Users/Ramesh Prashanth/Downloads/Onboarding 3.png (wireframe reference)
- C:/Users/Ramesh Prashanth/Downloads/Onboarding 4.png (wireframe reference)
- C:/Users/Ramesh Prashanth/Downloads/spalsh screen.png (splash reference -- already built)

<interfaces>
Design tokens available in globals.css @theme block:
- --color-brand-pink: #E8527A (primary brand pink)
- --color-brand-pink-dark: #C93A62
- --color-brand-pink-light: #FF85A8
- --color-splash-bg-center: #FFFFFF
- --color-splash-bg-mid: #FFE8F0
- --color-splash-bg-edge: #FFCDE0
- --color-on-surface: #1A1A2E (dark text)
- --color-on-surface-secondary: #6B7280 (gray text)
- --color-surface: #FFFFFF
- --radius-xl: 1rem
- --radius-full: 9999px
- --spacing-* scale from xs (0.25rem) to 2xl (3rem)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite onboarding page to match design images</name>
  <files>app/onboarding/page.tsx, app/globals.css</files>
  <action>
READ all 5 design images listed in context FIRST before writing any code.

Rewrite `app/onboarding/page.tsx` to match the provided designs. The page must render at iPhone 16 Pro dimensions (393x852 logical pixels). Use the existing SVG illustration components unchanged.

**Welcome Screen (step 0) -- match the high-fidelity "Welcome Screen - iPhone 16 Pro.png" closely:**

Layout (top to bottom, centered, full-height flex column):
1. Decorative elements: A large semi-transparent pink circle (~120px) clipped at the top-right corner of the screen. A smaller one near bottom-left.
2. App icon: A ~80x80px rounded-square (border-radius ~20px) with a pink gradient background (from --color-brand-pink to --color-brand-pink-dark), containing a white medical cross (+) icon. Add a subtle shadow.
3. "MomCare" in large (~24px) bold pink text (--color-brand-pink). Below it "CLINIC MANAGEMENT" in small (~11px) uppercase letter-spaced gray text (--color-on-surface-secondary).
4. Illustration card: A rounded rectangle (~320px wide, ~200px tall) with a very light pink/beige background (#FFF5F0 or similar warm tone). The IllustrationWelcome component renders inside it, centered. Give the card rounded-xl corners and subtle shadow.
5. Category badge: A small pink dot (6px circle, --color-brand-pink) followed by "Maternal Healthcare" text in pink (~13px), centered horizontally.
6. Title: "Welcome to the\nMaternal Health Clinic" in large (~26px) bold dark text (--color-on-surface), centered, with line breaks.
7. Description: "Helping healthcare workers manage maternal clinics, patient records, and antenatal care efficiently." in ~14px gray text (--color-on-surface-secondary), centered.
8. Button: Full-width (with horizontal padding ~24px) pink pill button (--color-brand-pink background, white text, border-radius: 9999px / full, ~52px height). Text "Get Started" with a right arrow. Font-weight semibold.
9. Pagination dots: 3 dots below the button. First dot is dark/elongated (active), others are small circles in light gray. Use the existing dot style but match the design's 3-dot count (the design shows 3 dots for the Welcome + 2 more screens, but we have 4 screens total -- use 4 dots to match the actual screen count).
10. NO Skip/Next/Back buttons on the Welcome screen.

**Screens 1-3 (steps 1, 2, 3) -- follow the wireframe layouts (Onboarding 2-4.png):**

Layout (top to bottom):
1. Medical cross icon at top: A smaller version (~40x40px) rounded-square with pink gradient and white cross, centered.
2. Title: Screen-specific title in bold (~20px) dark text, centered. Titles: "Manage Pregnant Mothers" (step 1), "Manage Clinic Sessions" (step 2), "Monitor Maternal Health" (step 3).
3. Illustration: The corresponding SVG component (IllustrationMothers, IllustrationSessions, IllustrationHealth), centered with vertical spacing.
4. Description: Screen-specific description text in gray, centered.
5. Button: "Get Started" in a rounded rectangle button (not pill -- use border-radius ~12px, full-width, ~48px height, pink background, white text). On the last screen this navigates to /login.
6. Pagination dots: 4 dots, with the current one active (dark + elongated).
7. Navigation row at bottom:
   - Step 1: "Skip" on left (navigates to /login), "Next >" on right
   - Step 2: "Back" on left (goes to previous), "Next >" on right
   - Step 3: "Back" on left, no Next (Get Started button handles navigation to /login)

**Background for all screens:** Use the existing radial gradient from splash-bg-center through splash-bg-mid to splash-bg-edge.

**Shared behavior:**
- Use `useState` for step tracking (0-3)
- Use `useRouter` for navigation to /login
- Smooth transitions between screens (CSS transition or simple state swap)
- Use design tokens from globals.css -- no hardcoded color values
- Keep all `style={{ }}` inline usage for CSS custom properties as the existing code does (Tailwind v4 does not support var() in arbitrary values consistently)

**In globals.css**, add any new tokens needed (e.g., illustration card background color). Add to the @theme block:
```
--color-illustration-bg: #FFF5F0;
```
Also add any keyframe animations if needed for screen transitions.

Do NOT modify the SVG illustration component files -- import and use them as-is.
  </action>
  <verify>
    <automated>cd "D:/UX projects/MomCare frontend" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Onboarding page builds without errors. Welcome screen layout matches the high-fidelity design (app icon, branding, illustration card, badge, title, button, dots). Screens 1-3 follow wireframe structure. Navigation logic works correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify onboarding screens match design images</name>
  <files>app/onboarding/page.tsx</files>
  <action>
User visually compares the rendered onboarding screens against the provided design images at iPhone 16 Pro (393x852) viewport.

Steps:
1. Run `cd "D:/UX projects/MomCare frontend" && npm run dev`
2. Open Chrome DevTools, set device to iPhone 16 Pro (393x852)
3. Navigate to http://localhost:3000/onboarding
4. Compare Welcome screen (step 0) against: "C:/Users/Ramesh Prashanth/Downloads/Welcome Screen - iPhone 16 Pro.png"
   - Check: App icon with gradient and cross, "MomCare" + "CLINIC MANAGEMENT", illustration in rounded card, "Maternal Healthcare" badge, large title, description, pink pill button with arrow, pagination dots, decorative pink circles
5. Tap "Get Started" or "Next" to cycle through screens 1-3
6. Compare each screen layout against the wireframes (Onboarding 2.png, 3.png, 4.png)
7. Verify Skip goes to /login, Back goes to previous screen, final Get Started goes to /login
8. Check overall color palette uses the pink brand colors consistently
  </action>
  <verify>User confirms screens match designs or provides specific feedback</verify>
  <done>User types "approved" confirming all 4 screens match the provided designs, or describes issues for iteration</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- All 4 screens render correctly at 393x852 viewport
- Welcome screen visually matches the high-fidelity design image
- Navigation flow: Welcome -> Screen 1 -> Screen 2 -> Screen 3 -> /login
- Skip shortcut works from screens 1-2
- Back navigation works from screens 2-3
</verification>

<success_criteria>
The 4 onboarding screens closely match the provided design images when viewed at iPhone 16 Pro dimensions. The Welcome screen matches the high-fidelity design with all visual elements (app icon, branding, illustration card, badge, title, pill button, dots). Screens 1-3 follow the wireframe structure. Navigation between screens and to /login works correctly.
</success_criteria>

<output>
After completion, create `.planning/quick/260318-r1d-develop-the-4-onboarding-screens-based-o/260318-r1d-SUMMARY.md`
</output>
